# Практическое задание — Устройство map, slice и channel в Go

---

## Часть 1 — Map

Создай файл `map.md` и ответь на вопросы:

1. Что такое хэш-таблица и какие три операции она должна поддерживать?
2. Что такое коллизия и почему её невозможно полностью избежать?
3. Чем метод цепочек отличается от открытой адресации?
4. Что такое load factor и что происходит когда он превышен?
5. Какую структуру имеет бакет в старой реализации map Go? Что хранится в top hash и зачем?
6. Почему ключи и значения хранятся раздельно (сначала все ключи, потом все значения), а не парами?
7. Что такое эвакуация данных? Почему она инкрементальная, а не происходит сразу?
8. Почему нельзя взять указатель на элемент map?
9. Почему порядок обхода map в Go случайный? Это баг или фича?
10. Зачем заранее выделять память под map через `make(map[K]V, n)`?
11. Чем новая реализация map (SwissTable, Go 1.24) отличается от старой?
12. Что такое Control Word в SwissTable и как он ускоряет поиск?
13. Как H1 и H2 используются в SwissTable при поиске ключа?
14. Что такое расширяемое хэширование (extendible hashing) и зачем оно нужно?
15. Что такое SIMD и почему его использование ускоряет поиск в SwissTable?

---

## Часть 2 — Slice

Создай файл `slice.md` и ответь на вопросы:

1. Чем slice отличается от массива в Go?
2. Из каких трёх полей состоит заголовок slice?
3. Что такое базовый массив? Могут ли два slice разделять один базовый массив?
4. Что происходит когда `append` превышает ёмкость slice?
5. Чем `len` отличается от `cap`?
6. Чему равно нулевое значение slice? Чем `nil` slice отличается от пустого `[]int{}`?
7. Почему при передаче slice в функцию изменение элементов затрагивает оригинал, а `append` — нет?
8. Зачем использовать `make([]T, 0, n)` вместо `var s []T` если знаешь размер заранее?
9. Как правильно скопировать slice чтобы изменения копии не затрагивали оригинал?
10. В чём проблема со slice поверх большого массива и как её решить?
11. Почему результат `append` нужно всегда присваивать той же переменной?
12. Что происходит с `cap` при создании среза через `s[a:b]`?

---

## Часть 3 — Channel

Создай файл `channel.md` и ответь на вопросы:

1. Какие четыре ключевых свойства обеспечивает канал?
2. Из каких полей состоит внутренняя структура канала?
3. Что такое кольцевая очередь и как она используется в буферизованном канале?
4. Что происходит с горутиной когда она пытается записать в переполненный канал?
5. Что такое очереди `sendq` и `recvq`? Что в них хранится?
6. Что такое структура `sudog` и какие поля в ней есть?
7. Как происходит прямая передача данных (direct send) минуя буфер канала?
8. Чем небуферизованный канал отличается от буферизованного на уровне реализации?
9. Что происходит при закрытии канала с горутинами в `sendq` и `recvq`?
10. Как реализована конструкция `select` под капотом?
11. Почему в `select` порядок обхода `case` случайный?
12. Почему канал использует мьютекс, а не lock-free структуру?
13. Что вернёт чтение из закрытого канала?
14. Что произойдёт если записать в закрытый канал?